// (c) 2026 GoElev8.ai | Aaron Bryant. All rights reserved. Unauthorized use prohibited.
//
// PLACEHOLDER — Aaron is sending the real goelev8-sms-calculator.jsx
// component. When the file arrives we swap this whole page's contents
// for that component. The page is fully self-contained (no nav, no
// footer, no shared layout chrome) per spec.

import SmsCalcClient from './SmsCalcClient';

export const metadata = {
  title: 'SMS Pricing & Calculator | GoElev8.ai',
  description: 'Figure out how many SMS credits your business needs.',
};

export default function SmsCalcPage() {
  return <SmsCalcClient />;
}
